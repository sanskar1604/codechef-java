# Subarray with 0, 1, 2

## 📌 Problem Statement

You are given an array `arr` consisting only of integers `0`, `1`, and `2`.  

Your task is to **find the number of contiguous subarrays** that contain **at least one occurrence of each integer** (`0`, `1`, and `2`).

---

## 🧾 Input Format

1. The first line contains an integer `n` — the size of the array.
2. The second line contains `n` space-separated integers — elements of the array:  
   `arr[0], arr[1], …, arr[n-1]`.

---

## 📤 Output Format

- Print a single integer — the count of all subarrays that contain **0, 1, and 2**.

---

## 🔒 Constraints

- `1 ≤ n ≤ 10^5`
- `0 ≤ arr[i] ≤ 2`

---

## 🧪 Sample Input 1

4
0 1 2 0

## 🧪 Sample Output 1

3
